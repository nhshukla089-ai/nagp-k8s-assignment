# Don't put real passwords here.
# Create at deploy time:
#
# kubectl create secret generic postgres-secret \
#   --namespace nagp-assignment \
#   --from-literal=POSTGRES_PASSWORD='...' \
#   --from-literal=DB_PASSWORD='...'
